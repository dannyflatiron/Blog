<h1>Edit <%= @post.title%>!</h1>

<%= render partial: "layouts/errors", locals: {object: @post} %>

<%= form_for(@post) do |f|%>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <br><br>
  <%= f.label :content %>
  <%= f.text_area :content %>


  <%= f.submit %>

<% end %>